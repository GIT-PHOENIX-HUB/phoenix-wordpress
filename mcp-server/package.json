{
  "name": "phoenix-wordpress-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server exposing the phoenixelectric.life WordPress site via the WP REST API (Application Password auth). Local stdio server for Claude Desktop / Coworker.",
  "bin": {
    "phoenix-wordpress-mcp": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  }
}
